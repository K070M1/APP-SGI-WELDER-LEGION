export enum Rol {
  ADMIN = 'ADMIN',
  FINANZAS = 'FINANZAS',
  ALMACEN = 'ALMACEN',
  VENDEDOR = 'VENDEDOR',
}

export enum TipoMovimiento {
  ENTRADA = 'ENTRADA',
  SALIDA = 'SALIDA',
  AJUSTE = 'AJUSTE',
}